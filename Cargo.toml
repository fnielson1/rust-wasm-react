[package]
name = "wasm-test"
version = "0.1.0"
edition = "2024"

[lints.clippy]
shadow_reuse = "deny"
shadow_same = "deny"
shadow_unrelated = "deny"


[dependencies]
wasm-bindgen = "^0.2"
web-sys = { version = "^0.3", features = ["console"] }
js-sys = "^0.3"
libm = "^0.2"

[lib]
crate-type = ["cdylib", "rlib"]
